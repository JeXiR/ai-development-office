# AI Development Office v2.1.4 — CallMe Validation CLI Hotfix

Fixes the only blocker found after v2.1.3 RC2 passed completely: top-level `await` in the CallMe validation CLI under CJS `tsx` output.

Created and maintained by **JeXiR (Halil Cinkilinc)**.
