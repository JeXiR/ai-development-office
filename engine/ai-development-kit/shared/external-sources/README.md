# External UI / Effect Sources

This registry lists approved starting points for component/effect discovery.

Important:
- approval means "allowed to evaluate", not "blindly copy"
- license must still be checked
- existing project components/dependencies take priority
- external code must be adapted to project architecture/design system

Canonical registry:
`shared/external-sources/registry.json`
