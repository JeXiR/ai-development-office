---
name: accessibility
description: Build accessible web interfaces with semantic HTML, keyboard support, visible focus, labels and motion preferences.
---

# Accessibility

- Prefer semantic HTML.
- Every input needs an accessible name.
- Interactive elements must be keyboard reachable.
- Preserve visible focus.
- Do not use color alone to convey state.
- Provide meaningful alt text for informative images.
- Keep dialogs focus-managed and dismissible appropriately.
- Respect `prefers-reduced-motion`.
- Use ARIA only when native semantics cannot express the behavior.
