# Capability Registry

Capabilities are modular dependency/config units.

They are intentionally smaller than presets.

Examples:
- preset = Next.js + Prisma + Postgres + Docker
- capability = add Motion
- capability = add shadcn/ui
- capability = add S3 storage

Canonical registry:
`shared/capabilities/registry.json`
