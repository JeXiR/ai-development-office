# Presets

Presets define a starting architecture profile and initial skill set.

They do not replace project analysis.
After starter code is created, run `analyze-project.ps1` and `sync-project.ps1`.

Canonical registry:
`shared/presets/registry.json`
