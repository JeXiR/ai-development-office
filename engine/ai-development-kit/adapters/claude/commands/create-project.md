# Create Project

Use:
- preset-selector
- template-generator
- project-bootstrap

Select the closest preset from `shared/presets/registry.json`.
Generate the AI baseline and docs.
After framework scaffolding, run project analysis and project sync.
