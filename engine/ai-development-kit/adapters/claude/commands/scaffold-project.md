# Scaffold Project

1. run `check project readiness`
2. require READY
3. use `scaffold-composition.ps1` with resolved capabilities
4. validate each capability
5. run doctor
6. update project state

Do not use framework-specific mega preset scripts except deprecated wrappers.
