# v2 Integrations — rc.3

Integration registry supports MCP, GitHub, GitLab, Slack, Discord, generic webhooks, Jira, Linear, Notion, Sentry and CI endpoints.

Secrets are referenced by environment-variable name rather than persisted directly.

MCP server definitions are persisted under `.ai-kit/integrations/mcp-servers.json`.
