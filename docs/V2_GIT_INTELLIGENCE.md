# v2 Git Intelligence — rc.2

Features:
- local branch list / current branch / upstream ahead-behind
- commit graph history
- working-tree file summary
- guarded branch creation and checkout
- guarded commit
- project snapshots containing commit metadata and optional working-tree patch
- snapshot restore

All Git mutations require trusted project mode.
